# Work Queue

Priority order within each tier is not strict; use judgement per
`orchestrator/WARP_CYCLE_PROMPT.md` step 4 ("identify the highest-value
bounded problem").

- ~~Implement UART TX firmware (bit-banged via `SHIFTOUT`+`WAIT`) and verify
  against an independent Python UART receiver model~~ - done: `tools/uart_model.py`
  (UartReceiver + build_uart_tx_asm) + `test/test_uart.py` (verified across
  edge cases, pseudorandom frames, and periods 4, 8, 16).
- ~~Add per-opcode isolated unit tests~~ - done: `test/test_opcodes.py` directly
  exercises all ALU, branch, loop, GPIO, and shift instructions.

## P0

- Rename `tt_um_change_me_protocol_emulator` to include the real GitHub
  username, once known, in `src/project.v`, `info.yaml`, and `test/tb.v`.

## P1

- ~~Add a bootloader checksum/CRC and a way to signal a failed/short load
  back to the host (see `docs/limitations.md`)~~ - done: hardware CRC-8 (poly 0x07)
  accumulator in `src/core.v`, `uo_out[1:0]` status reporting, permanent halt on mismatch,
  `test/test_bootload.py` (5/5 tests), SymbiYosys formal proof.
- ~~Add an RVFI-style per-cycle verification interface ("PVFI", debug-only,
  gated by a `SIM`/`PVFI` define) and a first SymbiYosys formal harness
  under `formal/`: "PC stays in valid range", "reset reaches a known
  state", "WAIT/bootloader-load sequences terminate"~~ - done: `src/core.v`
  PVFI ports gated under `ifdef PVFI, formal harness `formal/core.sby` +
  `formal/core_formal.v` verified 20 steps with Z3 (0 violations).
- ~~Add `WAITEDGE rd, imm8` (stall for an edge on the GPIO bus, capture
  elapsed cycles into `rd`) plus a free-running cycle counter - a genuine
  autobaud/timing-discovery primitive for reverse-engineering unknown
  protocols, and something RP2040 PIO's documented total lack of runtime
  observability cannot do~~ - done: `src/core.v` opcode 21, 32-bit cycle counter,
  `test/test_waitedge.py` pulse-width measurement to single-cycle precision.
- ~~Run first Yosys synthesis pass; record mapped cell count/area breakdown
  in `docs/ppa.md` and `orchestrator/metrics.json`~~ - done: `scripts/synth.sh` +
  `scripts/synth.ys`, mapped 19,143 CMOS cells (37,542 GE), 1,402 cells in active processor core.
- ~~Add a randomized instruction-stream fuzzer that runs against both the
  RTL and `tools/isa_model.py`, with automatic shrink-on-failure~~ - done:
  `tools/fuzzer.py` + `test/test_fuzz.py`, automated 1-minimization shrinker.

## P2

- ~~Mutation testing harness: deliberately invert a branch condition /
  off-by-one a `WAIT` counter / wrong ALU op / dropped reset, confirm the
  test suite catches it, and report a measured kill rate~~ - done:
  `scripts/mutate.py`, 10/10 mutants killed (100.0% kill rate) in 46.95s.
- ~~SPI Master firmware supporting all 4 modes (CPOL 0/1, CPHA 0/1) and full-duplex~~ -
  done: `tools/spi_model.py` (build_spi_master_asm + SpiSlave) + `test/test_spi.py` (Mode 0-3, full duplex, random).
- ~~I2C firmware (START, STOP, ACK/NACK, open-drain primitive OP_GODRI/OP_GODR)~~ -
  done: `src/core.v`, `src/gpio.v`, `tools/i2c_model.py` (build_i2c_write_asm + build_i2c_read_asm + I2cSlave) + `test/test_i2c.py` (5/5 tests pass, open-drain contention prevention formally proven).
- ~~I2C Clock Stretching & Multi-Master Arbitration Detection via `WAITEDGE` & `GRD`~~ -
  done: `tools/i2c_model.py` (build_i2c_write_with_stretch_asm, build_i2c_write_with_arbitration_asm) + `test/test_i2c.py` (22/22 regression pass, stretch absorbed, arbitration loss cleanly aborted).
- ~~Bootloader CRC-8 checksum addition to detect corrupted/truncated frames~~ -
  done: on-chip hardware CRC-8 accumulator, `test/test_bootload.py` (5/5 tests), SymbiYosys proven, 12/12 mutants killed.
- ~~UART RX firmware with start-bit edge synchronization via `WAITEDGE`~~ -
  done: `tools/uart_model.py` (build_uart_rx_asm + UartTransmitter) + `test/test_uart.py` (31/31 regression pass, zero jitter, framing error & glitch rejection, 13/13 mutants killed).
- ~~1-Wire Master Protocol Engine (Dallas DS18B20 Timing) with presence pulse discovery and bit timeslots via open-drain and `WAITEDGE`~~ -
  done: `tools/onewire_model.py` (OneWireSlave + build_onewire_reset_presence_asm + build_onewire_read_byte_asm + build_onewire_write_byte_asm) + `test/test_onewire.py` (35/35 regression pass, presence duration sweep, read/write timeslots, 14/14 mutants killed).
- ~~PS/2 Bidirectional Host Controller (device clock edge sync on falling clock, 11-bit odd-parity verified frame reception and host-to-device inhibit/send via open-drain and `WAITEDGE`)~~ -
- ~~JTAG TAP Controller Engine (TMS state machine: Test-Logic-Reset, Run-Test/Idle, Shift-DR, Shift-IR, BYPASS and IDCODE readout)~~ -
- ~~ARM SWD (Serial Wire Debug) Interface Engine (Line Reset sequence 50+ clocks high, JTAG-to-SWD switching, turnaround bits, SWD header and ACK readout)~~ -
- ~~Manchester Biphase-L (IEEE 802.3 / MIL-STD-1553) Encoder & Decoder Engine~~ -
  done: `tools/manchester_model.py` (ManchesterDecoder + ManchesterTransmitter + build_manchester_tx_asm + build_manchester_rx_asm) + `test/test_manchester.py` (57/57 regression pass, 4-cycle symmetric half-bit waveform, standard/pseudorandom RX into R0, biphase violation detection, 18/18 mutants killed).
- ~~CAN Bus Physical-Layer Controller (Dominant/Recessive Bit Timing, Bit Stuffing & Arbitration via `GRD`)~~ -
  done: `tools/can_model.py` (CanReceiverModel + compute_can_crc15 + build_can_tx_asm + build_can_rx_asm) + `test/test_can.py` (62/62 regression pass, bit stuffing, CRC-15, in-cell arbitration loss detection R2=0xAA, missing ACK R2=0xAE, dominant ACK assertion, 19/19 mutants killed).
- ~~DMX512 Stage Lighting Protocol Engine (Break pulse >= 88us, MAB, Start Code 0x00, and slot reception with per-slot edge sync)~~ -
  done: `tools/dmx512_model.py` (Dmx512ReceiverModel + build_dmx512_tx_packet_asm + build_dmx512_rx_slot_asm) + `test/test_dmx512.py` (67/67 regression pass, Break pulse duration measurement in R3, per-slot WAITEDGE edge resynchronization, 20/20 mutants killed).
- ~~Pure Firmware Autobaud Rate Auto-Discovery Engine (pulse duration discovery via WAITEDGE, symmetry validation, multi-profile classification, and adaptive sampling)~~ -
  done: `tools/autobaud_model.py` (AutobaudTransmitterModel + build_autobaud_rx_asm) + `test/test_autobaud.py` (75/75 regression pass, rate 8/16/32 profiles, noise rejection, framing error detection, 21/21 mutants killed).
- ~~Program RAM architecture trade-off study (`program_ram.v` combinational vs synchronous read)~~ -
  done: quantified >80 ns timing slack at 10 MHz (<12 ns combinational path delay across 20 logic levels), justified retaining combinational read to guarantee 1-cycle determinism without pipeline bubbles or branch penalty stalls.
- ~~HDLC / SDLC Bit-Oriented Protocol Engine (ISO/IEC 13239 bit-oriented framing, NRZI line coding, zero-bit stuffing/destuffing, flag framing 0x7E, abort sequences)~~ -
  done: `tools/hdlc_model.py` (HdlcTransmitter + HdlcReceiver + build_hdlc_tx_words + build_hdlc_rx_words) + `test/test_hdlc.py` (82/82 regression pass, zero jitter, bit stuffing on 0xFF/0x7E/0x3F, payload recovery in R0, abort detection R1=0xAB, 22/22 mutants killed).
- ~~Autonomous Hardware Protocol Sniffer & Dynamic Pattern Classifier Engine (passive pulse timing measurement via `WAITEDGE`, two's-complement bounds checking, automatic protocol fingerprinting into UART, Manchester, 1-Wire, DMX512, HDLC, or Noise)~~ -
  done: `tools/classifier_model.py` (TrafficGenerator + build_protocol_sniffer_asm) + `test/test_classifier.py` (89/89 regression pass, multi-protocol fingerprinting, noise rejection R0=0xFF, pin safety, 23/23 mutants killed).

## P3 (research / novelty, advanced validation)

- ~~Gate-level simulation with real standard-cell timing models (`GATES=yes`)~~ -
  done: `test/simcells_timing.v` (calibrated CMOS standard cell specify timing models: 50-80 ps gate, 200 ps clock-to-Q) + `test/test_gate_level.py` (8/8 tests pass in 23.10s: bootload, CRC-8 lock, UART TX, SPI Master, Manchester, DMX512, HDLC, open-drain bus safety) + `scripts/test_gl.sh`.
- ~~Deterministic protocol fault injection & protocol stress engine (intentional CAN stuff errors, CRC corruption, I2C collision, UART framing error, HDLC aborts)~~ -
  done: `tools/fault_injector_model.py` (generators for CAN stuff error, CRC-15 corruption, EOF dominant glitch, HDLC abort, stuff omission, corrupted flag, UART framing error, sub-baud glitch, Manchester biphase violation) + `test/test_fault_injection.py` (9/9 cocotb tests pass, 100% detection by independent protocol models, MUT_24 killed).
- ~~Multi-lane architecture investigation (dual-core protocol bridging, event fabric within 8x4 Tiny Tapeout footprint)~~ -
  done: `docs/multilane_study.md` (PPA feasibility proof: Split Memory 2x128x16 adds only 1,775 cells, +9.2% area overhead, ~40.5 kGE total, <65% placement density), `tools/multilane_model.py` (DualCoreSystem, single-cycle event fabric, lock-free mailbox with overflow/underflow protection, build_dual_core_bridge_asm), `test/test_multilane.py` (6/6 cocotb tests pass: concurrent execution, 1-cycle event strobe wakeup, lock-free mailbox transfer, overflow/underflow protection, end-to-end Manchester-to-SPI bridge, pin isolation), `MUT_25` killed.
- ~~Low-speed USB 1.1 physical signaling and packet framing engine~~ -
  done: `tools/usb_model.py` (differential line states J/K/SE0/SE1, NRZI modulation, dynamic bit stuffing and destuffing, CRC-5/CRC-16 algorithms, independent `UsbReceiver` cycle-accurate monitor, `build_usb_tx_packet_asm` transmitter, `build_usb_rx_packet_asm` receiver) + `test/test_usb.py` (6/6 cocotb tests pass: ACK/NAK/STALL handshakes, SETUP token with CRC-5, bit stuffing on 0xFF, multi-byte DATA0 sweeps, continuous SE0 bus reset detection, electrical safety), `MUT_26` killed.
- ~~End-to-end Protocol Sniff -> Classify -> Ingress -> Replay pipeline demo & cross-protocol bridge~~ -
  done: `tools/pipeline_model.py` (`build_pipeline_uart_echo_asm`, `build_pipeline_manchester_echo_asm`, `build_pipeline_uart_to_spi_bridge_asm`) + `test/test_pipeline.py` (4/4 cocotb tests pass: UART sniff->classify->echo, UART ingress to Lane 1 SPI Master egress bridge, noise rejection Class 0xFF, passive-to-active pin safety), `MUT_27` killed.
- ~~Automated Protocol Fuzzing & Anomaly Injection Campaign (combining constrained-random fuzzing with protocol fault injection models to stress test core recovery states)~~ -
  done: `tools/protocol_fuzzer.py` (`ProtocolFuzzer` with phase-bounded edge jitter, false-start glitch injection, framing error corruption, Manchester biphase violation, multi-frame recovery sequence generator) + `test/test_protocol_fuzz.py` (6/6 cocotb tests pass: UART jitter tolerance across pseudorandom payloads, UART false-start runt glitch rejection R2=0xFF, UART framing error detection R2=0xFE, Manchester jitter tolerance 0x96, multi-frame recovery Valid->Corrupted->Valid, and physical electrical safety uio_oe=0x00), `MUT_28` killed.
- ~~10 Mbit Ethernet physical signaling feasibility study (Manchester / NLP)~~ -
  done: `docs/ethernet_study.md` (PPA & physical feasibility study), `tools/ethernet_model.py` (`EthernetTransceiverModel` with NLP heartbeat generation, LSB-first Manchester packet framing, IEEE 802.3 CRC-32 FCS calculation, `build_ethernet_nlp_generator_asm`, `build_ethernet_tx_packet_asm`, `build_ethernet_rx_packet_asm`) + `test/test_ethernet.py` (6/6 cocotb tests pass: NLP periodic heartbeats, TX packet framing with SFD 0xD5 and TP_IDL, RX SFD sync and payload ingress, IEEE 802.3 CRC-32 FCS, NLP link-loss detection, pin direction electrical safety), `MUT_29` killed.
- ~~Multi-Protocol Bus Bridging Matrix (I2C-to-SPI, UART-to-CAN, 1-Wire-to-UART, Multi-Byte Streaming)~~ -
  done: `tools/bridge_matrix_model.py` (`build_bridge_i2c_master_to_spi_asm`, `build_bridge_uart_to_can_asm`, `build_bridge_onewire_to_uart_asm`, `build_bridge_multi_byte_stream_asm`) + `test/test_bridge_matrix.py` (6/6 cocotb tests pass: I2C read to SPI Master Mode 0 egress, UART RX to open-drain CAN 2.0A egress with ACK monitoring, 1-Wire read timeslots to UART TX 8-N-1 egress, multi-byte UART-to-SPI streaming across [0x11, 0x22, 0x33], ingress framing error isolation suppressing spurious CAN transmission, and unused pin High-Z electrical safety), `MUT_30` killed.
- ~~Hardware Watchdog Timer & Brownout Recovery Circuit Feasibility Study~~ -
  done: `docs/watchdog_study.md` (PPA, BOD supervisor co-design, and WWDT feasibility study), `tools/watchdog_model.py` (`WatchdogModel` cycle-accurate simulator with windowed bounds, two-token FSM, sticky reset registers; firmware generators for periodic service, task hang, early-pet window violation, brownout recovery) + `test/test_watchdog.py` (5/5 cocotb tests pass: normal WWDT servicing, task deadlock timeout and soft reset, windowed early-pet violation trap, brownout warm-boot recovery in 3 cycles, reset electrical isolation), `MUT_31` killed.
- ~~Dynamic Power & Energy Optimization Study (Clock Gating & Instruction Micro-Architectural Profiling)~~ -
  done: `docs/power_study.md` (PPA and CMOS power physics study on IHP SG13G2 130nm standard cells), `tools/power_model.py` (`PowerModel` cycle-accurate energy tracker with IHP 130nm parameters, clock gating models, and benchmark firmware generators) + `test/test_power.py` (6/6 cocotb tests pass: instruction power profiling, wait stall clock gating 98.68% reduction, waitedge low-power stall with 1-cycle instant wakeup, pad capacitive load linear energy scaling 20pF vs 50pF, UART protocol energy benchmark 104 pJ/bit, halted state electrical safety), `MUT_32` killed.
- ~~Cryptographic Accelerator Feasibility Study (ChaCha8 / Poly1305 / SHA-256 bit-sliced microcode vs. hardware coprocessor)~~ -
  done: `docs/crypto_study.md` (PPA trade-off analysis on RFC 8439 ChaCha8/20, Poly1305, and SHA-256 microcode vs hardware coprocessor macro on IHP 130nm), `tools/crypto_model.py` (`CryptoPerformanceModel`, ChaCha8 ARX quarter-round, SHA-256 Ch/Maj primitives, Poly1305 MAC step, firmware generators) + `test/test_crypto.py` (6/6 cocotb tests pass: 32-bit addition with carry ripple, ChaCha quarter-round step, Poly1305 MAC step, SHA-256 Ch/Maj primitives, coprocessor PPA scaling 33x to 44x speedup, electrical pin isolation), `MUT_33` killed.
- ~~Deterministic Real-Time Task Scheduling Engine (Priority Multi-Tasking & Round-Robin Schedulers)~~ -
  done: `docs/scheduler_study.md` (real-time scheduling theory, cooperative vs preemptive models, worst-case response latency bounds $R \le 17$ cycles / $1.7\,\mu\text{s}$, round-robin time slicing, zero-gate PPA analysis), `tools/scheduler_model.py` (`SchedulerModel` cycle-accurate reference simulator, `Task`, `TaskContext`, firmware generators for cooperative priority, round-robin fairness, context switch fidelity, and hard deadline periodic execution) + `test/test_scheduler.py` (6/6 cocotb tests pass: cooperative priority preemption, round-robin fairness across 3 tasks, context switch fidelity, hard periodic deadline compliance with `WAIT`, WCRL latency bounds, GPIO bus electrical isolation), `MUT_34` killed.
- ~~CAN FD Flexible Data-Rate Protocol Accelerator Feasibility & Bit-Rate Switching Study~~ -
  done: `docs/canfd_study.md` (ISO 11898-1:2015 specification, single-cycle dual-rate switching mechanics, DLC 0-15 encoding, CRC-17/21 polynomials, and PPA analysis on IHP 130nm), `tools/canfd_model.py` (`CanFdFrame`, `CanFdReceiver`, `CanFdPerformanceModel`, `compute_canfd_crc17`, `compute_canfd_crc21`, and dual-rate/streaming firmware generators) + `test/test_canfd.py` (6/6 cocotb tests pass: single-cycle dual-rate switching, 64-byte payload streaming, CRC-17/21 mathematical validation with 100% single-bit error detection, BRS=0 classical CAN fallback compatibility, hardware coprocessor PPA scaling with 5.76x speedup, open-drain bus electrical safety), `MUT_35` killed.
- ~~Physical Die Floorplan, Pad Placement & Package Pinout Co-Design Study~~ -
  done: `docs/floorplan_study.md` (die geometry 1x2 and 2x2 footprints, standard cell placement density 58.4% in 2x2 tile, sg13cmos5l_io pad cell allocation, QFN-64 package pinout, SSO ground bounce modeling, adjacent pin cross-talk isolation, and PDN IR drop budget), `tools/floorplan_model.py` (`PhysicalPad`, `SsoGroundBounceModel`, `IrDropModel`, `CrossTalkModel`, and firmware generators) + `test/test_floorplan.py` (6/6 cocotb tests pass: SSO 8-pin simultaneous switching stability, adjacent pin drive isolation, analytical SSO ground bounce bounding V_bounce=64 mV < 200 mV noise margin, PDN IR drop bounding V_drop=7.77 mV < 0.45% VDD, adjacent pin cross-talk capacitive coupling factor Kc <= 0.0076 / >42 dB isolation, clean return to High-Z on halt), `MUT_36` killed.
- ~~Asynchronous Event Notification & Level/Edge Interrupt Controller Subsystem~~ -
  done: `docs/interrupt_study.md` (asynchronous event handling theory, HIC dual-rank synchronizer architecture, trigger mode selector, priority arbitration, vector dispatch, latency analysis, and PPA scaling on IHP 130nm SG13G2), `tools/interrupt_model.py` (`TriggerMode`, `InterruptChannel`, `InterruptControllerModel`, `InterruptPpaModel`, `build_edge_event_capture_asm`, `build_level_event_handler_asm`, `build_priority_event_dispatcher_asm`, `build_nested_context_preservation_asm`) + `test/test_interrupt.py` (6/6 cocotb tests pass: single-cycle rising edge event capture with timestamp in R3, level-sensitive IRQ detection and ACK handshake, strict priority arbitration between Pin 0 and Pin 1, nested register context preservation, cycle-accurate HIC model and PPA validation, and High-Z electrical pin safety), `MUT_37` killed.
- ~~Hardware-Assisted Cyclic Redundancy Check (CRC-16/CRC-32) Coprocessor Macro PPA Feasibility Study~~ -
  done: `docs/crc_study.md` (parallel GF(2) matrix compression LFSR formulation, multi-polynomial support for CRC-16/CCITT, CRC-16/MODBUS, CRC-32/IEEE 802.3, residual match constant monitoring, 64x throughput speedup, and synthesizable coprocessor macro PPA scaling on IHP 130nm SG13G2), `tools/crc_model.py` (`CrcPolynomialMode`, `compute_crc16_ccitt`, `compute_crc16_modbus`, `compute_crc32_ieee`, `CrcCoprocessorModel`, `CrcPpaModel`, and firmware generators) + `test/test_crc.py` (6/6 cocotb tests pass: software bitwise Galois CRC accumulation matching RFC reference, coprocessor single-cycle byte streaming, mathematical multi-polynomial RFC vector validation, single-bit error detection and fault code trapping R2=0xCE, coprocessor PPA scaling model, and High-Z pin electrical safety), `MUT_39` killed.
- ~~I3C (MIPI I3C v1.1.1) Sensor Protocol & Dynamic Address Assignment (DAA) Acceleration Engine~~ -
  done: `docs/i3c_study.md` (MIPI I3C v1.1.1 specification, dynamic open-drain to active push-pull line switching physics, ENTDAA broadcast dynamic address assignment, 48-bit Provisional ID wired-AND arbitration, In-Band Interrupt latency bounding, and synthesizable coprocessor macro PPA scaling on IHP 130nm SG13G2), `tools/i3c_model.py` (`I3cTargetDevice`, `I3cPpaModel`, and firmware generators) + `test/test_i3c.py` (6/6 cocotb tests pass: broadcast CCC ENEC with target ACKs, full ENTDAA dynamic address assignment, multi-target Provisional ID open-drain arbitration, active push-pull SDR data transfer, In-Band Interrupt event detection R2=0x1B, coprocessor PPA scaling model, and High-Z pin electrical safety), `MUT_40` killed.
- ~~Quadrature Encoder Interface (QEI) & Industrial Motion Feedback Engine~~ -
  done: `docs/qei_study.md` (quadrature phase decoding theory, forward/reverse Gray code state sequences, 1X/2X/4X resolution modes, digital debounce filtering, and synthesizable peripheral macro PPA scaling on IHP 130nm SG13G2), `tools/qei_model.py` (`QeiResolutionMode`, `QeiDirection`, `QeiEncoder`, `QeiDecoder`, `QeiPpaModel`, and firmware generators) + `test/test_qei.py` (6/6 cocotb tests pass: forward rotation counting +4, reverse rotation counting -4 / 0xFC, bidirectional motion +3 CW -2 CCW net +1, index homing calibration R2=1 R3=0x5A, velocity period estimation R3=43 cycles via WAITEDGE, and PPA validation), `MUT_41` killed.
- ~~LIN (Local Interconnect Network) Automotive Protocol Engine & Break-Sync Frame Processor~~ -
  done: `docs/lin_study.md` (automotive single-wire open-drain bus physics, Break/Sync timing, PID P0/P1 mixed parity, Classic and Enhanced ones' complement carry-wrap checksums, and synthesizable coprocessor macro PPA scaling on IHP 130nm SG13G2), `tools/lin_model.py` (`compute_lin_pid`, `verify_lin_pid`, `compute_lin_checksum`, `LinFrame`, `LinSlaveModel`, `LinPpaModel`, and firmware generators) + `test/test_lin.py` (6/6 cocotb tests pass: master frame transmission with >=13-bit Break, slave Break pulse duration capture via WAITEDGE R0=104 cycles, mathematical PID parity validation across all 64 IDs, Classic and Enhanced ones' complement checksum verification, slave payload ingress, and PPA scaling with open-drain safety), `MUT_42` killed.
- ~~High-Speed Synchronous Serial Interface (SSI / BiSS-C) Absolute Rotary Encoder Engine~~ -
  done: `docs/biss_study.md` (SSI and BiSS-C specifications, Single-Cycle Data framing, mathematical Gray-to-binary formulation, CRC-6 LFSR equations, and IHP 130nm PPA scaling), `tools/biss_model.py` (`gray_to_binary`, `binary_to_gray`, `compute_biss_crc6`, `verify_biss_crc6`, `SsiEncoderModel`, `BissEncoderModel`, `BissPpaModel`, and firmware generators) + `test/test_biss.py` (6/6 cocotb tests pass: ALU Gray-to-binary decoding across 10 vectors, SSI master position sampling R0=0xD4, BiSS-C frame acquisition R0=0x9B, R1=0x03, R2=0x20, CRC-6 polynomial integrity and corruption detection, fault ingress for nE/nW flags R1=0x00, and PPA scaling), `MUT_43` killed.
- ~~MIL-STD-1553B Avionic Multiplex Data Bus Dual-Redundant Protocol Engine~~ -
  done: `docs/mil1553_study.md` (MIL-STD-1553B Notice 2 avionic specification, 20-bit word structure, 3-bit non-Manchester sync waveform physics, dual-redundant bus failover mechanics, RT response latency bounds, and IHP 130nm PPA scaling), `tools/mil1553_model.py` (`compute_1553_parity`, `verify_1553_parity`, `build_command_word`, `parse_command_word`, `build_status_word`, `Mil1553RemoteTerminal`, `Mil1553PpaModel`, and firmware generators) + `test/test_mil1553.py` (6/6 cocotb tests pass: BC Command Word waveform transmission with 24-cycle sync, RT command reception with address match and Status Word generation, mathematical odd parity validation with 100% single-bit error rejection, RT address filtering trapping mismatches with 0xEE, BC dual-redundant automatic failover to Bus B with 0xBB status code, and PPA scaling), `MUT_44` killed.
- ~~Wiegand Security Access Control Protocol Reader/Writer & Pulse Width Discovery Engine~~ -
  done: `docs/wiegand_study.md` (Wiegand magnetic wire effect physics, 26-bit standard framing H10301, active-low pulsed signaling, even/odd parity structures, WAITEDGE pulse width discovery, physical line short / tamper detection, and IHP 130nm SG13G2 PPA scaling), `tools/wiegand_model.py` (`compute_wiegand26_parity`, `build_wiegand26_raw`, `verify_wiegand26`, `WiegandReaderModel`, `WiegandPpaModel`, and firmware generators) + `test/test_wiegand.py` (6/6 cocotb tests pass: 26-bit credential transmission FC=102 ID=34567, WAITEDGE pulse width and interval discovery T_pw=12 T_pi=25 cycles, mathematical 26-bit parity validation with 100% single-bit error rejection, 8-bit stream ingress into R0 via GRD polling, line short tamper detection with status code R2=0xAA, and PPA scaling), `MUT_45` killed.
- ~~ARINC 429 Mark 33 Digital Information Transfer System (DITS) Avionic Protocol Engine~~ -
  done: `docs/arinc429_study.md` (ARINC 429 Mark 33 DITS avionic specification, 32-bit word framing, dual-rail bipolar Return-to-Zero BPRZ signaling, octal label conversion, SDI/SSM semantics, odd parity tree, and IHP 130nm SG13G2 PPA scaling), `tools/arinc429_model.py` (`compute_arinc429_parity`, `build_arinc429_word`, `parse_arinc429_word`, `octal_to_label`, `Arinc429ReceiverModel`, `Arinc429PpaModel`, and firmware generators) + `test/test_arinc429.py` (6/6 cocotb tests pass: 32-bit word transmission with dual-rail BPRZ pulses on TXA/TXB, label filter match with status code R2=0x00, label mismatch rejection with error code R2=0xEE, SDI filtering with status code R2=0x00, line fault short detection with alarm code R2=0xAA, and mathematical odd parity validation across 32 bits with PPA scaling), `MUT_46` killed.
- ~~MIDI 2.0 (Universal MIDI Packet - UMP) High-Resolution Synthesizer & Control Protocol Engine~~ -
  done: `docs/midi2_study.md` (MIDI 2.0 UMP architecture, 32/64/96/128-bit packet formats, MT classification, 16-group routing, high-resolution 16-bit velocity and 32-bit pitch bend, JR timestamps, UART 8-N-1 serialization, and IHP 130nm SG13G2 PPA scaling), `tools/midi2_model.py` (`build_ump_32`, `build_ump_midi1_note_on`, `build_ump_midi2_note_on`, `build_ump_midi2_pitch_bend`, `build_ump_jr_timestamp`, `parse_ump_word`, `parse_ump_64`, `Midi2PpaModel`, and firmware generators) + `test/test_midi2.py` (6/6 cocotb tests pass: 32-bit UMP packet transmission serialized as 4 UART 8-N-1 octets on pin 3, group filtering match R2=0x00 R1=3, group mismatch rejection R2=0xEE, note dispatch match R2=0x00 R0=60, note mismatch rejection R2=0xEE, and high-resolution voice & JR timestamp validation with PPA model), `MUT_47` killed.
- ~~I2S (Inter-IC Sound) & TDM Digital Audio Multi-Channel Serial Interface Engine~~ -
  done: `docs/i2s_study.md` (I2S Philips specification, continuous SCK, WS channel selection, 1-bit standard delay vs Left-Justified alignment, TDM multi-slot framing, in-register volume attenuation, and IHP 130nm SG13G2 PPA scaling), `tools/i2s_model.py` (`I2sReceiverModel`, `I2sPpaModel`, `build_i2s_tx_master_asm`, `build_i2s_rx_slave_asm`, `build_i2s_volume_scale_asm`, `build_tdm_slot_filter_asm`) + `test/test_i2s.py` (6/6 cocotb tests pass: I2S Master transmission Left=0xA5 Right=0x3C verified against I2sReceiverModel, I2S Slave stereo reception Left=0x5A into R0 Right=0xC3 into R1, in-register volume attenuation 6 dB and 12 dB in R0, TDM multi-channel Slot 2 extraction 0x33 into R0, I2S standard delay phase discrimination, and audio standards & PPA scaling validation), `MUT_48` killed.
- ~~SpaceWire (ECSS-E-ST-50-52C) Data-Strobe (DS) Spacecraft Serial Bus Protocol Engine~~ -
  done: `docs/spacewire_study.md` (SpaceWire ECSS-E-ST-50-52C Data-Strobe specification, LVDS physical signaling physics, clock recovery without PLL $\Delta D \oplus \Delta S = 1$, 4-bit Control Characters, 10-bit Data Characters, odd character parity rule, composite tokens NULL/Time-Code, credit-based flow control +8 accounting, and IHP 130nm SG13G2 PPA scaling), `tools/spacewire_model.py` (`SpaceWireReceiverModel`, `SpaceWirePpaModel`, `build_spacewire_tx_packet_asm`, `build_spacewire_rx_char_asm`, `build_spacewire_rx_token_asm`, `build_spacewire_credit_tracker_asm`) + `test/test_spacewire.py` (6/6 cocotb tests pass: 3-character packet transmission D=0xA5, EOP, D=0x3C, single-character reception with parity validation R0=0x5A R2=0x00, parity error detection R2=0xEE, composite token reception for FCT R0=0x04, credit tracker accounting +8 credits R0=8, and PPA scaling model), `MUT_49` killed.
- ~~SAE J2716 SENT Automotive Sensor Protocol Engine & Nibble Pulse-Period Modulation Engine~~ -
  done: `docs/sent_study.md` (SAE J2716 specification, falling-to-falling edge pulse-period modulation PPM, 56-tick calibration pulse clock recovery $t_{tick} = T_{sync} / 56$, nibble duration decoding $T = (12 + N) \times t_{tick}$, mathematical CRC-4 polynomial $x^4 + x^3 + x^2 + 1$ with seed 5, and IHP 130nm SG13G2 PPA scaling), `tools/sent_model.py` (`compute_sent_crc4`, `verify_sent_crc4`, `SentFrame`, `SentReceiverModel`, `SentPpaModel`, `build_sent_tx_frame_asm`, `build_sent_rx_sync_and_nibble_asm`, `build_sent_crc4_validator_asm`) + `test/test_sent.py` (6/6 cocotb tests pass: SENT frame transmission decoded by SentReceiverModel into Status 0, Data [1,2,3,4,5,6], CRC 12, sync pulse & nibble duration measurement via WAITEDGE 560 cycles and 170 cycles with R2=0x00, mathematical CRC-4 polynomial validation, in-register CRC-4 validator firmware, pause pulse handling, and PPA scaling model), `MUT_50` killed.
- ~~EtherCAT (Ethernet for Control Automation Technology - IEC 61158) Sub-Datagram Processing & "Processing-on-the-Fly" Engine~~ -
  done: `docs/ethercat_study.md` (IEC 61158 specification, sub-datagram structure, addressing modes, 'Processing-on-the-Fly' latency physics, WKC accounting rules, and IHP 130nm SG13G2 PPA scaling), `tools/ethercat_model.py` (`EtherCatCommand`, `EtherCatSubDatagram`, `EtherCatSlaveModel`, `EtherCatPpaModel`, `build_ethercat_slave_process_asm`, `build_ethercat_tx_datagram_asm`, `build_ethercat_wkc_incrementer_asm`, `build_ethercat_address_filter_asm`) + `test/test_ethercat.py` (6/6 cocotb tests pass: sub-datagram master transmission, configured station matching with on-the-fly WKC increment, station address mismatch bypass, broadcast write handling, 16-bit multi-byte WKC carry propagation, and PPA model validation), `MUT_51` killed.
- ~~Profibus DP (Decentralized Peripherals - IEC 61158 / EN 50170) Master/Slave Fieldbus Protocol Engine~~ -
  done: `docs/profibus_study.md` (IEC 61158 / EN 50170 specification, RS-485 physical signaling, HD=4 Hamming distance proof, FCS mathematical formulations, and IHP 130nm SG13G2 PPA scaling), `tools/profibus_model.py` (`ProfibusDelimiter`, `compute_profibus_fcs`, `ProfibusTelegram`, `ProfibusSlaveModel`, `ProfibusPpaModel`, `build_profibus_tx_sd2_asm`, `build_profibus_slave_rx_asm`, `build_profibus_token_filter_asm`, `build_profibus_fcs_validator_asm`) + `test/test_profibus.py` (6/6 cocotb tests pass: SD2 telegram master transmission, slave configured address match and payload latching, destination address mismatch bypass, FCS error trapping, SD4 token reception and predecessor capture, and PPA model validation), `MUT_52` killed.
- ~~Ethernet AVB / TSN (Audio Video Bridging / Time-Sensitive Networking - IEEE 802.1Qav / IEEE 802.1Qbv) Protocol Engine & Credit-Based Shaper~~ -
  done: `docs/tsn_study.md` (IEEE 802.1Q-2018, IEEE 802.1Qav CBS algorithm, IEEE 802.1Qbv TAS gate control, IEEE 1722 AVTP encapsulation, and IHP 130nm SG13G2 PPA scaling), `tools/tsn_model.py` (`TsnTrafficClass`, `TsnEtherType`, `TsnVlanTag`, `TsnFrame`, `CreditBasedShaperModel`, `TsnPpaModel`, `build_tsn_tx_vlan_frame_asm`, `build_tsn_rx_priority_classifier_asm`, `build_tsn_cbs_credit_shaper_asm`, `build_tsn_gate_control_asm`) + `test/test_tsn.py` (6/6 cocotb tests pass: 802.1Q tagged frame transmission, ingress PCP priority classification for Class A, Class B, and Best Effort, CBS credit depletion/recovery with TAS gate control, and PPA model validation), `MUT_53` killed.
- ~~Modbus RTU / ASCII (IEC 61158 / Modbus-IDA) Protocol Engine & Serial Bus Master/Slave Controller~~ -
  done: `docs/modbus_study.md` (Modbus-IDA specification, IEC 61158 serial profiles, RTU silence intervals t3.5/t1.5, CRC-16/MODBUS reversed polynomial, ASCII LRC formulation, exception architecture, and IHP 130nm SG13G2 PPA scaling), `tools/modbus_model.py` (`ModbusFunctionCode`, `ModbusExceptionCode`, `compute_modbus_crc16`, `compute_modbus_lrc`, `ModbusRtuFrame`, `ModbusAsciiFrame`, `ModbusSlaveModel`, `ModbusPpaModel`, `build_modbus_rtu_tx_frame_asm`, `build_modbus_rtu_slave_rx_asm`, `build_modbus_ascii_tx_frame_asm`, `build_modbus_lrc_validator_asm`, `build_modbus_exception_generator_asm`) + `test/test_modbus.py` (6/6 cocotb tests pass: Modbus RTU master transmission, slave configured address match and payload latching, address mismatch bypass, Modbus ASCII master transmission, in-register LRC and exception generation, and PPA model validation), `MUT_54` killed.
- ~~FlexRay (ISO 17458) Automotive Deterministic Bus Protocol Engine & Dual-Channel TDMA Controller (static segment TDMA time slotting, dynamic minislotting, Header CRC-11, Frame CRC-24, dual-channel redundancy Channel A/B, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/flexray_study.md` (ISO 17458-1/2/3 specification, communication cycle structure, static TDMA segment, Header CRC-11, Frame CRC-24, dual-channel redundancy, and IHP 130nm SG13G2 PPA scaling), `tools/flexray_model.py` (`compute_flexray_header_crc11`, `compute_flexray_frame_crc24`, `FlexRayFrame`, `FlexRayNodeModel`, `FlexRayPpaModel`, `build_flexray_tx_frame_asm`, `build_flexray_tdma_slot_tracker_asm`, `build_flexray_rx_filter_asm`, `build_flexray_header_crc_validator_asm`, `build_flexray_dual_channel_failover_asm`) + `test/test_flexray.py` (6/6 cocotb tests pass: FlexRay frame master transmission, TDMA slot engine timing, Frame ID filter matching, Frame ID mismatch bypass, dual-channel failover, and CRC/PPA validation), `MUT_55` killed.
- ~~CANopen (CiA 301 / EN 50325-4) & SAE J1939 Higher-Layer Automotive/Industrial Protocol Engine (CANopen NMT network management state machine, SDO expedited transfer, PDO cyclic transmission, Heartbeat producer/consumer, SAE J1939 29-bit identifier PGN parameter group extraction, Transport Protocol BAM multi-packet reassembly, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/canopen_study.md` (CiA 301 and SAE J1939 specifications, NMT state machine, Heartbeat protocol, SDO expedited transfer protocol, 29-bit CAN-ID structure, PDU1 vs PDU2 formatting, BAM multi-packet transport protocol, and IHP 130nm SG13G2 PPA scaling), `tools/canopen_model.py` (`CanOpenNmtCommand`, `CanOpenNmtState`, `CanOpenSdoCs`, `CanOpenFrame`, `J1939Frame`, `CanOpenNodeModel`, `CanOpenPpaModel`, `build_canopen_nmt_state_machine_asm`, `build_canopen_heartbeat_generator_asm`, `build_canopen_sdo_expedited_transfer_asm`, `build_j1939_pgn_extractor_asm`, `build_j1939_bam_reassembly_asm`) + `test/test_canopen.py` (6/6 cocotb tests pass: NMT state transitions, NMT node address filtering and bypass, Heartbeat producer frame generation, SDO expedited transfer match & abort, SAE J1939 29-bit CAN-ID PDU1/PDU2 addressing, and CANopen/J1939 standards & PPA validation), `MUT_56` killed.
- ~~IEEE 1588 PTP (Precision Time Protocol / IEEE 1588-2019 / IEC 61588) Hardware Timestamping & Sub-Microsecond Clock Synchronization Engine (PTP message parsing: Sync, Follow_Up, Delay_Req, Delay_Resp; hardware ingress/egress timestamp capture via WAITEDGE timestamp mode; syntonization/frequency ratio drift compensation; Mean Path Delay and Master-to-Slave Offset estimation; PPA scaling on IHP 130nm SG13G2)~~ -
  done: `docs/ptp_study.md` (IEEE 1588-2019 specification, physical-layer SFD hardware timestamping, two-step clock mode, mean path delay and clock offset math, syntonization drift equations, and IHP 130nm SG13G2 PPA scaling), `tools/ptp_model.py` (`PtpMessageType`, `PtpClockMode`, `compute_mean_path_delay`, `compute_clock_offset`, `compute_syntonization_ratio`, `PtpHeader`, `PtpClockModel`, `PtpPpaModel`, `build_ptp_sync_tx_asm`, `build_ptp_rx_timestamp_asm`, `build_ptp_offset_calculator_asm`, `build_ptp_message_filter_asm`) + `test/test_ptp.py` (6/6 cocotb tests pass: PTP Sync master transmission with egress timestamping, physical-layer ingress timestamp capture via WAITEDGE, mean path delay and offset calculation in microcode, message type classification and filtering, syntonization drift ratio mathematical validation, and PTP standards & PPA validation), `MUT_57` killed.
- ~~I3C Basic / MIPI I3C v1.2 HDR-DDR (High Data Rate Double Data Rate) Multi-Drop Protocol Engine (HDR-DDR double-edge clocking, 18/20-bit word transfers with 2-bit preamble, 2-bit HDR parity, CRC-5 Galois polynomial framing, HDR Exit sequence detection, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/i3c_hdr_study.md` (MIPI I3C v1.2 specification, HDR-DDR dual-edge clocking physics, 18/20-bit word framing, 2-bit preamble, CRC-5 Galois polynomial math, ENTHDR CCC, HDR Exit pattern, and IHP 130nm SG13G2 PPA scaling), `tools/i3c_hdr_model.py` (`HdrPreamble`, `compute_i3c_crc5`, `verify_i3c_crc5`, `compute_hdr_parity`, `compute_hdr_word_parity`, `build_hdr_word_bits`, `I3cHdrTargetModel`, `I3cHdrPpaModel`, `build_i3c_hdr_tx_word_asm`, `build_i3c_hdr_rx_word_asm`, `build_i3c_hdr_preamble_filter_asm`, `build_i3c_hdr_crc5_validator_asm`, `build_i3c_hdr_exit_asm`) + `test/test_i3c_hdr.py` (6/6 cocotb tests pass: HDR-DDR word master transmission with dual-edge SCL clocking, slave word ingress via WAITEDGE any-edge mode into R0/R1, preamble validation and mismatch trapping, CRC-5 polynomial validation with corruption detection, HDR Exit sequence detection, and PPA scaling), `MUT_58` killed.
- ~~USB 2.0 Full-Speed (12 Mbps) NRZI, Dynamic Bit Stuffing/Destuffing, and PID Packet Engine (differential D+/D- signaling, SOP/EOP delimiters, SYNC field 0x80, token/data/handshake PID complement verification, CRC-5/CRC-16 validation, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/usb_fs_study.md` (USB 2.0 Full-Speed specification, differential line physics, J/K/SE0/SE1 definitions, NRZI modulation, dynamic bit stuffing/destuffing, 15 standard PIDs, Token CRC-5, Data CRC-16, and IHP 130nm SG13G2 PPA scaling), `tools/usb_fs_model.py` (`verify_usb_pid`, `compute_usb_crc5`, `verify_usb_crc5`, `compute_usb_crc16`, `verify_usb_crc16`, `insert_usb_fs_bit_stuffing`, `remove_usb_fs_bit_stuffing`, `nrzi_encode_fs`, `nrzi_decode_fs`, `UsbFsPacket`, `UsbFsReceiverModel`, `UsbFsPpaModel`, `build_usb_fs_tx_packet_asm`, `build_usb_fs_rx_packet_asm`, `build_usb_fs_pid_validator_asm`, `build_usb_fs_eop_detector_asm`) + `test/test_usb_fs.py` (6/6 cocotb tests pass: DATA0 master packet transmission with SYNC 0x80, PID 0xC3, payload 0x5A, CRC-16, and EOP; slave packet ingress via WAITEDGE SOP synchronization capturing PID into R0 and payload into R1; in-register PID validation and fault trapping; dynamic bit stuffing/destuffing on consecutive 1s; EOP detection and SE0 bus reset tracking; and standards compliance & PPA scaling validation), `MUT_59` killed.
- ~~Ethernet 100BASE-TX (IEEE 802.3u Fast Ethernet) 4B5B Block Coding, Stream Cipher Scrambler, and MLT-3 Physical Sublayer Engine (4B5B nibble encoding/decoding, 11-bit LFSR pseudo-random stream cipher scrambler/descrambler $x^{11} + x^9 + 1$, three-level Multi-Level Transmit MLT-3 signaling +1/0/-1, Carrier Sense / Collision Detect, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/ethernet_100base_tx_study.md` (IEEE 802.3u Clause 24/25 specification, 4B/5B block coding, 11-bit LFSR scrambler math, MLT-3 ternary signaling physics, spectral compression, and IHP 130nm SG13G2 PPA scaling), `tools/ethernet_100base_tx_model.py` (`encode_4b5b_nibble`, `decode_4b5b_nibble`, `is_valid_4b5b_code`, `FastEthernetScrambler`, `mlt3_encode`, `mlt3_decode`, `Ethernet100BaseTxPacket`, `Ethernet100BaseTxReceiverModel`, `Ethernet100BaseTxPpaModel`, and firmware generators) + `test/test_ethernet_100base_tx.py` (6/6 cocotb tests pass: master packet transmission with /J/ /K/ SSD, payload [0x5A, 0xC3], /T/ /R/ ESD, and MLT-3 ternary signaling; slave delimiter detection and payload ingress via WAITEDGE into R0/R1; in-register 4B5B code group verification and fault trapping; 11-bit stream cipher scrambler/descrambler self-synchronization; carrier sense CRS detection on differential pair; and IEEE 802.3u standards compliance & PPA scaling validation), `MUT_60` killed.
- ~~Ethernet 1000BASE-T (IEEE 802.3ab Gigabit Ethernet) 4D-PAM5 Multilevel Signaling, 8B1Q4 Line Coding, and Physical Medium Attachment (PMA) Engine (4-dimensional 5-level pulse amplitude modulation -2/-1/0/+1/+2, 8B1Q4 8-bit to 4-quinary quads, hybrid echo cancellation, next-state trellis Viterbi decoding model, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/ethernet_1000base_t_study.md` (IEEE 802.3ab Clause 40 specification, 4D-PAM5 multilevel signaling physics, 8B1Q4 block coding, Trellis coset partitioning, 33-bit LFSR stream scrambler, SSD4/ESD4 delimiter framing, hybrid echo/crosstalk cancellation modeling, and IHP 130nm SG13G2 PPA scaling), `tools/ethernet_1000base_t_model.py` (`quantize_pam5`, `encode_8b1q4`, `decode_8b1q4`, `is_even_coset_d4d`, `GigabitEthernetScrambler`, `Ethernet1000BaseTPacket`, `Ethernet1000BaseTReceiverModel`, `Ethernet1000BaseTPpaModel`, and firmware generators) + `test/test_ethernet_1000base_t.py` (6/6 cocotb tests pass: master quad transmission with SSD4 delimiters, payload [0x5A, 0xC3], and ESD4 delimiters across pairs A/B; slave quad ingress via WAITEDGE into R0/R1; in-register 4D coset validation and fault trapping; 33-bit LFSR stream scrambler/descrambler matching; multilevel PAM5 voltage quantization; and IEEE 802.3ab standards compliance & PPA scaling validation), `MUT_61` killed.
- ~~USB 3.0 SuperSpeed (5.0 Gbps) Physical Layer & 8b/10b Link Training Engine (differential transmitter/receiver equalization, 8b/10b symbol encoding/decoding, running disparity tracking, Ordered Sets SKP/COM/PAD/SHP, Link Training and Status State Machine LTSSM polling/compliance, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/usb_ss_study.md` (USB 3.0 Chapter 6 physical layer specification, 8b/10b block coding math, running disparity equations, K-code definitions, TS1/TS2 ordered sets, SKP clock drift tolerance, LFPS physical signaling, LTSSM state machine, and IHP 130nm SG13G2 PPA scaling), `tools/usb_ss_model.py` (`encode_8b10b`, `decode_8b10b`, `is_comma_symbol`, `build_ts1_ordered_set`, `build_ts2_ordered_set`, `build_skp_ordered_set`, `UsbSsPacket`, `UsbSsReceiverModel`, `UsbSsPpaModel`, `generate_lfps_burst`, `verify_lfps_burst`, and firmware generators) + `test/test_usb_ss.py` (6/6 cocotb tests pass: master TS1 ordered set transmission with differential signaling on TXP/TXN; slave comma synchronization and symbol ingress via WAITEDGE into R0/R1; in-register symbol disparity validation and fault trapping; 8-pulse LFPS burst generation with anti-phase toggling; ordered sets TS1/TS2/SKP round-trip decoding and clock drift compensation; and USB 3.0 standards compliance & PPA scaling validation), `MUT_62` killed.
- ~~PCI Express Base Gen 1 (2.5 GT/s) Physical Layer & 8b/10b Link Engine (differential transmitter/receiver de-emphasis, 8b/10b symbol line coding, running disparity tracking, Ordered Sets TS1/TS2/SKP/FTS/IDL, Link Training and Status State Machine LTSSM Detect/Polling/Configuration, Fast Training Sequences FTS for L0s exit, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/pcie_gen1_study.md` (PCIe Base Specification physical layer, 8b/10b block coding math, running disparity equations, PCIe K-codes, TS1/TS2 ordered sets, SKP clock tolerance margin, FTS L0s exit sequences, 16-bit LFSR scrambler math, LTSSM state machine, and IHP 130nm SG13G2 PPA scaling), `tools/pcie_gen1_model.py` (`encode_8b10b`, `decode_8b10b`, `is_comma_symbol`, `PcieScrambler`, `build_pcie_ts1_ordered_set`, `build_pcie_ts2_ordered_set`, `build_pcie_skp_ordered_set`, `build_pcie_fts_ordered_set`, `build_pcie_eios_ordered_set`, `PcieGen1Packet`, `PcieGen1ReceiverModel`, `PcieGen1PpaModel`, and firmware generators) + `test/test_pcie_gen1.py` (6/6 cocotb tests pass: master TS1 ordered set transmission with differential signaling on TXP/TXN; slave comma synchronization and symbol ingress via WAITEDGE into R0/R1; in-register FTS symbol validation and fault trapping; 16-bit LFSR data scrambler/descrambler stream verification and HW descrambling; ordered sets TS1/TS2/SKP/FTS/EIOS round-trip decoding and SKP clock drift compensation margin; and PCIe Base standards compliance & PPA scaling validation), `MUT_63` killed.
- ~~Ethernet 10GBASE-R (IEEE 802.3ae 10 Gbps) Physical Coding Sublayer (PCS) 64b/66b Engine (64b/66b transmission block coding, 2-bit sync headers 01/10, pseudo-random stream scrambler G(x) = x^58 + x^39 + 1, block lock state machine, test pattern generator, BER monitor, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/ethernet_10gbase_r_study.md` (IEEE 802.3ae Clause 49 specification, 64b/66b block framing math, 2-bit sync header Hamming distance, Block Type definitions, 58-bit self-synchronizing scrambler equations and self-lock proof, block lock state machine, and IHP 130nm SG13G2 PPA scaling), `tools/ethernet_10gbase_r_model.py` (`encode_64b66b_data`, `encode_64b66b_control`, `decode_64b66b`, `Ethernet10GScrambler`, `Ethernet10GDescrambler`, `Ethernet10GBlockLockModel`, `Ethernet10GReceiverModel`, `Ethernet10GPpaModel`, and firmware generators) + `test/test_ethernet_10gbase_r.py` (6/6 cocotb tests pass: master block transmission with sync header and lead data byte on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register 2-bit sync header validation and fault trapping; 58-bit self-synchronizing scrambler/descrambler round-trip and blind self-lock; standard Block Type filter and 64-block lock state machine; and 10GBASE-R standards compliance & PPA scaling validation), `MUT_64` killed.
- ~~Serial ATA Revision 3.0 (SATA 6.0 Gbps) Out-of-Band (OOB) Signaling & Frame Link Engine (COMRESET, COMINIT, COMWAKE burst timing, 8b/10b primitive signaling, ALIGNp, SYNCp, Frame Information Structure FIS framing, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/sata_gen3_study.md` (SATA 3.0 physical and link layer specification, OOB burst/quiet physics, 3:1 quiet window discrimination ratio, Link Layer 4-byte primitive structure, running disparity tracking, and IHP 130nm SG13G2 PPA scaling), `tools/sata_gen3_model.py` (`encode_8b10b`, `decode_8b10b`, `SataPrimitive`, `SataOobBurst`, `SataReceiverModel`, `SataPpaModel`, and firmware generators) + `test/test_sata_gen3.py` (6/6 cocotb tests pass: master OOB burst generation on TXP/TXN; WAITEDGE pulse measurement discriminating COMRESET and COMWAKE; 4-byte Link Layer primitive transmission on pin 3; in-register primitive filtering matching SYNC and trapping mismatch; 8b/10b primitive round-trip and running disparity tracking; and SATA 3.0 standards compliance & PPA scaling validation), `MUT_65` killed.
- ~~MIPI D-PHY v2.5 Physical Layer Engine (High-Speed DDR up to 4.5 Gbps, Low-Power Escape Mode, SoT/EoT sync sequence, Spaced-One-Hot line encoding, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/mipi_dphy_study.md` (MIPI D-PHY v2.5 physical layer specification, LP vs HS signaling physics, SoT/EoT sequence mechanics, Escape Mode entry sequence, Spaced-One-Hot bit encoding/decoding, LPDT commands, and IHP 130nm SG13G2 PPA scaling), `tools/mipi_dphy_model.py` (`encode_spaced_one_hot`, `decode_spaced_one_hot`, `DphyLineState`, `DphyPacket`, `DphyReceiverModel`, `DphyPpaModel`, and firmware generators) + `test/test_mipi_dphy.py` (6/6 cocotb tests pass: master SoT and data transmission on Dp/Dn; slave SoT sync ingress via WAITEDGE into R0/R1; master Escape mode entry and Spaced-One-Hot command transmission on Dp/Dn; in-register Escape command filtering matching LPDT 0xE1 and trapping mismatch; Spaced-One-Hot round-trip encoding/decoding across multi-byte payloads; and MIPI D-PHY v2.5 standards compliance & PPA scaling validation), `MUT_66` killed.
- ~~MIPI C-PHY v2.0 Physical Layer Engine (3-Phase symbol encoding, 16b/7t trio transmission, 5.7 Gbps per trio, wire state transitions, and IHP 130nm SG13G2 PPA scaling)~~ -
  done: `docs/mipi_cphy_study.md` (MIPI C-PHY v2.0 physical layer specification, 3-Phase balanced signaling physics, 6 canonical wire states, 5-symbol transition encoding, 16b/7t mapping mathematics, differential receiver sensing AB/BC/CA, embedded clock recovery, and IHP 130nm SG13G2 PPA scaling), `tools/mipi_cphy_model.py` (`CPhyWireState`, `WIRE_STATE_VOLTAGES`, `WIRE_STATE_GPIO`, `transition_to_state`, `state_transition_to_symbol`, `state_to_differential`, `differential_to_state`, `encode_16b7t`, `decode_7t16b`, `CPhyReceiverModel`, `CPhyPpaModel`, and firmware generators) + `test/test_mipi_cphy.py` (6/6 cocotb tests pass: master 7-symbol transmission on wires A/B/C; slave Wire A sync ingress via WAITEDGE into R0/R1; 16b/7t lossless round-trip mapping; differential receiver sensing & clock transition zero-crossing validation; in-register symbol transition filter matching and fault trapping; and MIPI C-PHY v2.0 standards compliance & PPA scaling validation), `MUT_67` killed.
- ~~DisplayPort 2.0 (DP 2.0 / UHBR 10/20 Gbps) Physical Layer & 128b/132b Link Training Engine (128b/132b block framing, 2-bit sync headers, scrambler, and link training)~~ -
  done: `docs/dp20_study.md` (DisplayPort 2.0/2.1 VESA specification, UHBR 10/13.5/20 line rates, 128b/132b line coding physics, 2-bit sync header Hamming distance, 2-bit header parity, 23-bit LFSR scrambler math, link training states, and IHP 130nm SG13G2 PPA scaling), `tools/dp20_model.py` (`DpSyncHeader`, `DpTrainingPattern`, `compute_header_parity`, `verify_header_parity`, `is_valid_sync_header`, `encode_128b132b`, `decode_128b132b`, `Dp20Scrambler`, `Dp20Descrambler`, `Dp20ReceiverModel`, `Dp20PpaModel`, and firmware generators) + `test/test_dp20.py` (6/6 cocotb tests pass: master block transmission on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register sync header validation and fault trapping; 23-bit scrambler/descrambler round-trip and HW descrambling; 128b/132b block framing & lock FSM; and DisplayPort 2.0 standards compliance & PPA scaling validation), `MUT_68` killed.
- ~~SAS-4 (Serial Attached SCSI 24G) Physical Layer & 128b/150b Interpacket Framing Engine~~ -
  done: `docs/sas4_study.md` (INCITS 534 SAS-4 specification, 24.0 Gbps physical signaling, 128b/150b framing architecture, 20-bit FEC parity math, 34-bit LFSR scrambler polynomial, 4-byte Dword primitives, and IHP 130nm SG13G2 PPA scaling), `tools/sas4_model.py` (`Sas4SyncHeader`, `Sas4Primitive`, `compute_fec_parity_20`, `is_valid_sync_header`, `encode_128b150b`, `decode_128b150b`, `Sas4Scrambler`, `Sas4Descrambler`, `Sas4ReceiverModel`, `Sas4PpaModel`, and firmware generators) + `test/test_sas4.py` (6/6 cocotb tests pass: master block transmission on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register sync header validation and fault trapping; 34-bit scrambler/descrambler round-trip and HW descrambling; 128b/150b block framing & lock FSM; and SAS-4 standards compliance & PPA scaling validation), `MUT_69` killed.
- ~~RapidIO v4.0 Physical Layer & 8b/10b Packet Exchange Engine~~ -
  done: `docs/rapidio_study.md` (RapidIO Trade Association / ISO/IEC 18372 specification, LP-Serial physical signaling, K28.5 comma synchronization, short control symbol framing, 5-bit CRC math, stype0/stype1 flow control, and IHP 130nm SG13G2 PPA scaling), `tools/rapidio_model.py` (`RapidIoKCode`, `RapidIoControlSymType`, `RapidIoFType`, `compute_rapidio_crc5`, `verify_rapidio_crc5`, `compute_rapidio_crc16`, `encode_control_symbol`, `decode_control_symbol`, `RapidIoReceiverModel`, `RapidIoPpaModel`, and firmware generators) + `test/test_rapidio.py` (6/6 cocotb tests pass: master control symbol transmission on pin 3; slave comma ingress via WAITEDGE into R0/R1; in-register stype response validation and fault trapping; CRC-5 and CRC-16 polynomial verification and HW checks; control symbol framing & link lock acquisition; and RapidIO standards compliance & PPA scaling validation), `MUT_70` killed.
- ~~InfiniBand HDR/NDR Physical & Link Layer Engine~~ -
  done: `docs/infiniband_study.md` (InfiniBand Architecture Specification Volume 2, HDR 200 Gbps 50G PAM4 / 64b/66b line coding, NDR 400 Gbps 100G PAM4 line coding, Link Heartbeat, Link Training Ordered Sets TS1/TS2, Packet Delimiters SOP/EOP, 16-bit Variant CRC VCRC and 32-bit Invariant CRC ICRC, and IHP 130nm SG13G2 PPA scaling), `tools/infiniband_model.py` (`InfiniBandOrderedSet`, `InfiniBandOpCode`, `compute_infiniband_vcrc16`, `compute_infiniband_icrc32`, `encode_infiniband_packet`, `decode_infiniband_packet`, `InfinibandReceiverModel`, `InfinibandPpaModel`, and firmware generators) + `test/test_infiniband.py` (6/6 cocotb tests pass: master TS1 transmission on pin 3; slave SOP ingress via WAITEDGE into R0/R1; in-register BTH opcode validation and fault trapping; dual VCRC-16 and ICRC-32 verification and HW slice check; ordered set framing & link lock acquisition; and InfiniBand standards compliance & PPA scaling validation), `MUT_71` killed.
- ~~Fibre Channel 32G/64G (FC-FS-5) Physical Layer Engine~~ -
  done: `docs/fibre_channel_study.md` (ANSI INCITS 545 FC-FS-5 specification, 32G/64G line rates, PAM4 and 64b/66b line coding, Ordered Set primitives, BB_Credit flow control, FC-CRC32 math, and IHP 130nm SG13G2 PPA scaling), `tools/fibre_channel_model.py` (`FibreChannelDelimiter`, `compute_fc_crc32`, `encode_fc_frame`, `decode_fc_frame`, `FibreChannelReceiverModel`, `FibreChannelPpaModel`, and firmware generators) + `test/test_fibre_channel.py` (6/6 cocotb tests pass: master primitive transmission on pin 3; slave comma ingress via WAITEDGE into R0/R1; in-register BB_Credit flow control tracking and underflow trapping; in-register SOF delimiter filtering; frame encapsulation with FC-CRC32 and receiver BB_Credit accounting; and Fibre Channel standards compliance & PPA scaling validation), `MUT_72` killed.
- ~~Coherent Accelerator Processor Interface (OpenCAPI / CXL) Physical Layer~~ -
  done: `docs/cxl_opencapi_study.md` (CXL 1.1/2.0/3.0/3.1 and OpenCAPI 3.0/4.0 specifications, 32.0/64.0 GT/s PCIe Gen 5/6 electrical PHY, CXL.io / CXL.cache / CXL.mem protocol multiplexing, FLIT framing, 16-bit FLIT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/cxl_opencapi_model.py` (`CxlProtocolType`, `compute_cxl_crc16`, `encode_cxl_flit`, `decode_cxl_flit`, `CxlReceiverModel`, `CxlPpaModel`, and firmware generators) + `test/test_cxl_opencapi.py` (6/6 cocotb tests pass: master FLIT header transmission on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register protocol filtering and fault trapping; in-register CRC-16 syndrome validation; FLIT framing & link lock acquisition; and CXL standards compliance & PPA scaling validation), `MUT_73` killed.
- ~~HyperTransport 3.1 Physical Layer & Link Protocol Engine~~ -
  done: `docs/hypertransport_study.md` (HyperTransport 1.0/2.0/3.0/3.1 specifications, 3.2 GHz DDR signaling, CAD/CTL framing, NOP/READ/WRITE/RESPONSE commands, virtual channel credit accounting, 32-bit CRC, and IHP 130nm SG13G2 PPA scaling), `tools/hypertransport_model.py` (`HtCommandType`, `compute_ht_crc32`, `encode_ht_packet`, `decode_ht_packet`, `HyperTransportReceiverModel`, `HyperTransportPpaModel`, and firmware generators) + `test/test_hypertransport.py` (6/6 cocotb tests pass: master packet header transmission on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register command filtering and fault trapping; in-register virtual channel credit accounting and underflow trapping; packet framing & link lock acquisition; and HyperTransport standards compliance & PPA scaling validation), `MUT_74` killed.
- ~~InfiniBand XDR/GDR & Ultra Ethernet Consortium (UEC) Transport Engine~~ -
  done: `docs/uec_transport_study.md` (UEC 1.0 specification, InfiniBand XDR 800G / GDR 1.6T physical signaling, packet multipath spraying, selective packet retransmission, congestion control, 32-bit CRC, and IHP 130nm SG13G2 PPA scaling), `tools/uec_transport_model.py` (`UecOpCode`, `compute_uec_crc32`, `encode_uec_packet`, `decode_uec_packet`, `UecReceiverModel`, `UecPpaModel`, and firmware generators) + `test/test_uec_transport.py` (6/6 cocotb tests pass: master packet header transmission on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register opcode filtering and fault trapping; in-register CWND credit accounting and underflow trapping; packet framing & link lock acquisition; and UEC standards compliance & PPA scaling validation), `MUT_75` killed.
- ~~Universal Chiplet Interconnect Express (UCIe 1.0/2.0) Die-to-Die Physical & Sideband Engine~~ -
  done: `docs/ucie_study.md` (UCIe 1.0/2.0 specification, standard and advanced packaging, sideband link training, lane repair sparing, 16-bit CRC, and IHP 130nm SG13G2 PPA scaling), `tools/ucie_model.py` (`UcieSidebandOpCode`, `compute_ucie_crc16`, `encode_ucie_sideband_packet`, `decode_ucie_sideband_packet`, `UcieReceiverModel`, `UciePpaModel`, and firmware generators) + `test/test_ucie.py` (6/6 cocotb tests pass: master sideband packet header transmission via SHIFTOUT on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register opcode filtering and fault trapping; in-register spare lane allocation tracking and underflow trapping; packet framing & link lock acquisition; and UCIe standards compliance & PPA scaling validation), `MUT_76` killed.
- ~~Bunch of Wires (BoW - OCP ODSA) & OpenHBI Die-to-Die Physical Layer Engine~~ -
  done: `docs/bow_study.md` (OCP ODSA BoW specification, BoW-Base vs BoW-Fast, OpenHBI bus interface, slice sparing and lane repair, 16-bit CRC-16, and IHP 130nm SG13G2 PPA scaling), `tools/bow_model.py` (`BowOpCode`, `compute_bow_crc16`, `encode_bow_packet`, `decode_bow_packet`, `BowReceiverModel`, `BowPpaModel`, and firmware generators) + `test/test_bow.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register opcode filtering and fault trapping; in-register slice spare wire allocation tracking and underflow trapping; packet framing & link lock acquisition; and BoW standards compliance & PPA scaling validation), `MUT_77` killed.
- ~~NVLink (NVIDIA High-Speed GPU Interconnect) Physical & Data Link Layer Engine~~ -
  done: `docs/nvlink_study.md` (NVLink 1.0 to 5.0 specifications, sub-link architecture, remote memory transactions, buffer credit flow control, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/nvlink_model.py` (`NvLinkOpCode`, `compute_nvlink_crc16`, `encode_nvlink_packet`, `decode_nvlink_packet`, `NvLinkReceiverModel`, `NvLinkPpaModel`, and firmware generators) + `test/test_nvlink.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register opcode filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing & link lock acquisition; and NVLink standards compliance & PPA scaling validation), `MUT_78` killed.
- ~~AXI4-Stream & TileLink On-Chip Streaming Fabric & Interconnect Engine~~ -
  done: `docs/axi_stream_study.md` (ARM AMBA 4 AXI4-Stream & SiFive TileLink v1.8.1 specifications, TVALID/TREADY handshakes, Channel A-E architecture, credit flow control, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/axi_stream_model.py` (`TileLinkOpCode`, `compute_axi_stream_crc16`, `encode_axi_stream_packet`, `decode_axi_stream_packet`, `AxiStreamReceiverModel`, `AxiStreamPpaModel`, and firmware generators) + `test/test_axi_stream.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register opcode filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing & link lock acquisition; and AXI4-Stream standards compliance & PPA scaling validation), `MUT_79` killed.
- ~~AXI4/AXI5 Memory-Mapped (AXI4-MM) On-Chip Interconnect & Burst Controller Engine~~ -
  done: `docs/axi_mm_study.md` (ARM AMBA 4 AXI4 & AMBA 5 AXI5 specifications, 5-channel architecture, FIXED/INCR/WRAP burst mathematics, wrap boundaries, credit flow control, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/axi_mm_model.py` (`AxBurst`, `AxResp`, `AxiMmChannel`, `compute_axi_mm_burst_addresses`, `compute_axi_mm_crc16`, `encode_axi_mm_packet`, `decode_axi_mm_packet`, `AxiMmReceiverModel`, `AxiMmPpaModel`, and firmware generators) + `test/test_axi_mm.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register channel filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing, burst address calculation & link lock acquisition; and AXI4/AXI5 standards compliance & PPA scaling validation), `MUT_80` killed.
- ~~AMBA AHB-Lite / APB4 Multi-Master Interconnect & Low-Power Peripheral Subsystem Engine~~ -
  done: `docs/ahb_apb_study.md` (ARM AMBA 3 AHB-Lite & AMBA APB4 specifications, pipelined address/data phases, SINGLE/INCR/WRAP burst mathematics, wrap boundaries, APB FSM, bridge wait states, credit flow control, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/ahb_apb_model.py` (`AhbBurst`, `AhbTrans`, `AhbSize`, `AhbResp`, `ApbResp`, `AhbApbOpCode`, `compute_ahb_burst_addresses`, `compute_ahb_apb_crc16`, `encode_ahb_apb_packet`, `decode_ahb_apb_packet`, `AhbApbReceiverModel`, `AhbApbPpaModel`, and firmware generators) + `test/test_ahb_apb.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register command filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing, burst address calculation & link lock acquisition; and AHB/APB standards compliance & PPA scaling validation), `MUT_81` killed.
- ~~Wishbone B4 & Avalon-MM On-Chip Interconnect & Pipelined Crossbar Engine~~ -
  done: `docs/wb_avalon_study.md` (Wishbone B4 & Avalon-MM specifications, classic vs pipelined registered feedback cycles, waitrequest and STALL_I mechanics, readdatavalid pipelining, incremental burst address calculation, crossbar bridge translation, credit flow control, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/wb_avalon_model.py` (`WbCycleType`, `WbResp`, `AvalonResp`, `WbAvalonOpCode`, `compute_avalon_burst_addresses`, `compute_wb_avalon_crc16`, `encode_wb_avalon_packet`, `decode_wb_avalon_packet`, `WbAvalonReceiverModel`, `WbAvalonPpaModel`, and firmware generators) + `test/test_wb_avalon.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register command filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing, burst address calculation & link lock acquisition; and Wishbone/Avalon standards compliance & PPA scaling validation), `MUT_82` killed.
- ~~ARM AMBA CHI & ACE Cache-Coherent Interconnect Engine~~ -
  done: `docs/amba_chi_study.md` (ARM AMBA CHI Issues B/C/D/E & AMBA 4 ACE specifications, REQ/RSP/DAT/SNP flit architecture, MOESI cache state transitions, snoop filters, Home Node directory tracking, credit flow control, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/amba_chi_model.py` (`MoesiState`, `AmbaChiChannel`, `AmbaChiOpCode`, `transition_moesi`, `compute_amba_chi_crc16`, `encode_amba_chi_flit`, `decode_amba_chi_flit`, `AmbaChiReceiverModel`, `AmbaChiPpaModel`, and firmware generators) + `test/test_amba_chi.py` (6/6 cocotb tests pass: master flit header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register opcode filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing, MOESI state transitions & link lock acquisition; and AMBA CHI/ACE standards compliance & PPA scaling validation), `MUT_83` killed.
- ~~HBM3 / HBM3e (IEEE 2445) High-Bandwidth Memory Physical Layer & Command Engine~~ -
  done: `docs/hbm3_study.md` (IEEE 2445 / JEDEC JESD238 specifications, 16 pseudo-channels, 1024-bit interface, decoupled Row/Column command buses, bank hierarchy 4 BG x 4 BA = 16 banks/PC, 256 banks total, bank state FSM, credit flow control, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/hbm3_model.py` (`Hbm3BankState`, `Hbm3OpCode`, `compute_hbm3_crc16`, `encode_hbm3_packet`, `decode_hbm3_packet`, `Hbm3ReceiverModel`, `Hbm3PpaModel`, and firmware generators) + `test/test_hbm3.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register command filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing, bank state transitions & receiver model validation; and HBM3 standards compliance & PPA scaling validation), `MUT_84` killed.
- ~~DDR5 / LPDDR5 (JEDEC JESD79-5 / JESD209-5) Physical Layer & Command Scheduler Engine~~ -
  done: `docs/ddr5_study.md` (JEDEC JESD79-5 DDR5 & JESD209-5 LPDDR5 specifications, dual 32-bit subchannels, 8 Bank Groups x 4 Banks hierarchy = 32 banks/subchannel, 64 banks total, bank state FSM, credit flow control, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/ddr5_model.py` (`Ddr5BankState`, `Ddr5OpCode`, `compute_ddr5_crc16`, `encode_ddr5_packet`, `decode_ddr5_packet`, `Ddr5ReceiverModel`, `Ddr5PpaModel`, and firmware generators) + `test/test_ddr5.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register command filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing, bank state transitions & receiver model validation; and DDR5 standards compliance & PPA scaling validation), `MUT_85` killed.
- ~~GDDR6 / GDDR6X (JEDEC JESD250) High-Speed Graphics Memory Physical Layer & Command Engine~~ -
  done: `docs/gddr6_study.md` (JEDEC JESD250 GDDR6 & GDDR6X specifications, dual 16-bit channels, NRZ vs PAM4 signaling, 4 Bank Groups x 4 Banks hierarchy = 16 banks/channel, 32 banks total, bank state FSM, credit flow control, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/gddr6_model.py` (`Gddr6BankState`, `Gddr6OpCode`, `compute_gddr6_crc16`, `encode_gddr6_packet`, `decode_gddr6_packet`, `Gddr6ReceiverModel`, `Gddr6PpaModel`, and firmware generators) + `test/test_gddr6.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register command filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing, bank state transitions & receiver model validation; and GDDR6 standards compliance & PPA scaling validation), `MUT_86` killed.
- ~~LPDDR4 / LPDDR4X (JEDEC JESD209-4) Low-Power High-Speed Memory Physical Layer & Command Engine~~ -
  done: `docs/lpddr4_study.md` (JEDEC JESD209-4 LPDDR4 & JESD209-4B/C LPDDR4X specifications, dual 16-bit channels, 6-bit DDR CA bus, 2-clock command encoding, LVSTL vs LVSTL_0.6 signaling, 8 banks per channel, 16 banks total, MWR masked write, bank state FSM, credit flow control, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/lpddr4_model.py` (`Lpddr4BankState`, `Lpddr4OpCode`, `compute_lpddr4_crc16`, `encode_lpddr4_packet`, `decode_lpddr4_packet`, `Lpddr4ReceiverModel`, `Lpddr4PpaModel`, and firmware generators) + `test/test_lpddr4.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register command filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing, bank state transitions & receiver model validation; and LPDDR4 standards compliance & PPA scaling validation), `MUT_87` killed.
- ~~DDR4 / DDR3 (JEDEC JESD79-4 / JESD79-3) Synchronous Dynamic RAM Physical Layer & Command Controller Engine~~ -
  done: `docs/ddr4_study.md` (JEDEC JESD79-4 DDR4 & JESD79-3 DDR3 specifications, 64-bit channel, 4 Bank Groups x 4 Banks, CA bus multiplexing, ACT_n/RAS_n/CAS_n/WE_n, CA Parity, POD12 vs SSTL signaling, bank state FSM, credit flow control, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/ddr4_model.py` (`Ddr4BankState`, `Ddr4OpCode`, `compute_ddr4_crc16`, `encode_ddr4_packet`, `decode_ddr4_packet`, `Ddr4ReceiverModel`, `Ddr4PpaModel`, and firmware generators) + `test/test_ddr4.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register command filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing, bank state transitions & receiver model validation; and DDR4 standards compliance & PPA scaling validation), `MUT_88` killed.
- ~~eMMC 5.1 / SD 6.0 UHS-II (JEDEC JESD84-B51 / SD Association) Non-Volatile Memory Bus & Card Protocol Engine~~ -
  done: `docs/emmc_study.md` (JEDEC JESD84-B51 eMMC 5.1 & SD 6.0 UHS-II specifications, CMD line, DAT bus, Data Strobe, hardware partitions, card lifecycle FSM, CQE credit flow control, 7-bit CRC-7, 16-bit CCITT CRC, and IHP 130nm SG13G2 PPA scaling), `tools/emmc_model.py` (`EmmcCardState`, `EmmcPartition`, `EmmcOpCode`, `compute_emmc_crc7`, `compute_emmc_crc16`, `encode_emmc_packet`, `decode_emmc_packet`, `EmmcReceiverModel`, `EmmcPpaModel`, and firmware generators) + `test/test_emmc.py` (6/6 cocotb tests pass: master packet header transmission via SHIFTOUT MSB-first on pin 3; slave sync ingress via WAITEDGE into R0/R1; in-register command filtering and fault trapping; in-register credit tracking and underflow trapping; packet framing, partitions & receiver model validation; and eMMC standards compliance & PPA scaling validation), `MUT_89` killed.
- UFS 3.1 / 4.0 (Universal Flash Storage / JEDEC JESD220) Mobile Storage Protocol Engine

---

### Task Queue Status: Continuous Engineering Loop Active (Iteration 86 Complete, Iteration 87 Queued)
*Per `AGENTS.md`: Continuous engineering project. When the explicit task queue becomes empty, enter **RESEARCH_AND_PROOF** mode: find verification gaps, attempt to falsify assumptions, improve formal proofs, improve PPA, test alternative architectures and investigate novel protocol capabilities.*








